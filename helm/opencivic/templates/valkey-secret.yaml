{{- if .Values.valkey.enabled }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "opencivic.fullname" . }}-valkey
  labels:
    app.kubernetes.io/name: {{ include "opencivic.name" . }}
    app.kubernetes.io/component: valkey
type: Opaque
stringData:
  password: {{ .Values.valkey.auth.password | quote }}
{{- end }}
