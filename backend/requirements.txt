# OpenCivic — Python dependencies
# All licences: MIT, Apache 2.0, BSD, or PostgreSQL
# Pin ALL versions — pip-audit runs in CI on every PR

# ── Web framework ──
fastapi==0.111.0
uvicorn[standard]==0.29.0
gunicorn==22.0.0
pydantic==2.7.1
pydantic-settings==2.2.1

# ── Database ──
sqlalchemy[asyncio]==2.0.30
asyncpg==0.29.0
alembic==1.13.1
psycopg2-binary==2.9.9
# pgbouncer runs as a separate Docker service — not a Python dependency

# ── Cache / Queue ──
redis==5.0.4  # redis-py client — works with Valkey
celery[redis]==5.4.0
flower==2.0.1

# ── Authentication ──
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
httpx==0.27.0

# ── AI / LLM ──
openai==1.30.1
anthropic==0.28.0
sentence-transformers==3.0.1
qdrant-client==1.9.1

# ── Object storage ──
boto3==1.34.109  # S3 / Minio
azure-storage-blob==12.19.1
google-cloud-storage==2.16.0

# ── Data processing ──
pandas==2.2.2
pyarrow==16.0.0
duckdb==0.10.3
chardet==5.2.0
openpyxl==3.1.2
xlrd==1.2.0
pdfplumber==0.11.1

# ── Connectors ──
oracledb==2.4.1
PyHive==0.7.0
thrift==0.20.0
thrift-sasl==0.4.3

# ── Security ──
cryptography==42.0.7

# ── Observability ──
structlog==24.2.0
opentelemetry-api==1.24.0
opentelemetry-sdk==1.24.0
opentelemetry-instrumentation-fastapi==0.45b0
opentelemetry-exporter-otlp-proto-grpc==1.24.0

# ── Testing ──
pytest==8.2.0
pytest-asyncio==0.23.7
pytest-cov==5.0.0
httpx==0.27.0  # Test client
factory-boy==3.3.0

# ── Dev tools ──
ruff==0.4.4
mypy==1.10.0
bandit==1.7.8
pip-audit==2.7.3
